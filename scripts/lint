#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

node ./scripts/check-jsdoc-coverage.cjs
node ./scripts/lint-generated.cjs --check

PATH="$PWD/node_modules/.bin:$PATH" exec ./node_modules/.bin/ultracite check
